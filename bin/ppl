#!/usr/bin/env ruby

require "pathname"

bin_file = Pathname.new(__FILE__).realpath
$:.unshift File.expand_path("../../lib", bin_file)

require "ppl/cli"

class String
    alias_method :each, :each_line
end

cli = Ppl::CLI.new ARGV
cli.run

