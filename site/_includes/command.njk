---
layout: page
itemtype: WebPage
eleventyComputed:
  title: "ppl {{ name }}"
  description: "{{ summary }}"
  breadcrumbs:
    - text: ppl
      href: /
    - text: commands
      href: /commands
    - text: "{{ name }}"
      href: "/commands/{{ name }}"
---

<pre class="synopsis"><code>{{ synopsis }}</code></pre>

{{ content | safe }}

{% if see_also | length > 0 %}
  {% include "see-also.njk" %}
{% endif %}

