---
layout: page
eleventyComputed:
  title: "ppl {{ name }}"
  description: "{{ summary }}"
  breadcrumbs:
    - text: ppl
      href: /
    - text: commands
      href: /commands
    - text: "{{ name }}"
      href: "/commands/{{ name }}"
---

<section>
  <pre><code>{{ synopsis }}</code></pre>
</section>

<section>
  {{ content | safe }}
</section>

{% if see_also | length > 0 %}
  {% include "see-also.njk" %}
{% endif %}

