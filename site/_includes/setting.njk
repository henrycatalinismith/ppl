---
layout: page
itemtype: WebPage
eleventyComputed:
  title: "ppl [{{ name }}] config"
  description: "{{ summary }}"
  breadcrumbs:
    - text: ppl
      href: /
    - text: settings
      href: /settings
    - text: "[{{ name }}]"
      href: "/settings/{{ name | replace('\"', '') | slug }}"
---

<pre class="synopsis"><code>{{ example }}</code></pre>

{{ content | safe }}

{% if see_also | length > 0 %}
  {% include "see-also.njk" %}
{% endif %}

